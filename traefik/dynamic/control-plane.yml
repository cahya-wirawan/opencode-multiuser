# Rendered by scripts/install.sh.
http:
  routers:
    control-plane:
      rule: "Host(`__BASE_DOMAIN__`)"
      entryPoints:
        - __TRAEFIK_ENTRYPOINT__
      service: control-plane
  services:
    control-plane:
      loadBalancer:
        servers:
          - url: "http://host.containers.internal:__CONTROL_PLANE_PORT__"
